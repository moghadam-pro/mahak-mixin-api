# Changelog

این پروژه از [Semantic Versioning](https://semver.org/) پیروی می‌کند.

## [Unreleased]

## [0.1.1] - 2026-09-21

- عمومی‌سازی داشبورد گزارش داخلی بدون لاگین و حذف هدر و خروج.
- افزودن کنترل تم روشن/تاریک، بهبود فوتر و اصلاح نمایش کنسول و آیکن‌ها.
- افزودن لوگوهای محلی محک و میکسین و رفع سرویس‌دهی فونت و assetها در router وب.
- افزودن `noindex` و هدر `X-Robots-Tag` برای داشبورد داخلی.
- به‌روزرسانی README و قرارداد API داخلی متناسب با رفتار جدید داشبورد.

## [0.1.0] - 2026-09-20

- آغاز نسخه‌گذاری رسمی با فایل واحد `VERSION`، نمایش نسخه در داشبورد و خروجی health/CLI.
- بازطراحی پوسته داشبورد با ظاهر تمام‌تیره، لبه‌های تیز، رنگ‌های solid، هدر بدون لوگو و کارت یکپارچه وضعیت APIها.

- بازطراحی mobile-first داشبورد با رابط مدرن، navigation دسکتاپ، کارت‌های موبایل، نمای کنسولی فعالیت‌ها و آیکن‌های SVG محلی بدون dependency یا CDN جدید.
- افزایش جزئیات گزارش اجراها: جهت و موجودیت، زمان شروع و پایان، مدت اجرا، آمار تجمیعی، نرخ موفقیت، خطاها و ۱۰۰ رکورد اخیر.
- تأکید روشن در رابط کاربری بر فقط‌خواندنی بودن داشبورد و نبود کنترل اجرای sync.
- عمومی‌سازی مستندات با حذف نام تجاری اضافی، نام کاربر، دامنه اختصاصی و وابستگی به کنترل‌پنل خاص؛ افزودن تصویر داشبورد.
- افزودن خروجی PNG باکیفیت از راهنمای فارسی مشتری برای ارسال مستقیم.
- افزودن کارت وضعیت اتصال API محک و میکسین با refresh دستی و نمایش حالت سرویس «فعال (فقط پل)».
- بازنویسی کامل README به زبان فارسی و افزودن راهنمای کوتاه PDF مشتری با فونت وزیرمتن.
- استفاده از نسخه self-hosted فونت وزیرمتن در تمام صفحات ورود و داشبورد، بدون وابستگی به CDN.
- افزودن داشبورد RTL سبک با ورود session/password hash، آمار mapping، checkpoint و تاریخچه اجرای محلی.
- افزودن retry با exponential backoff برای عملیات امن؛ create محصول و تصویر برای جلوگیری از duplicate retry کور نمی‌شوند.
- تأیید اجرای production عمومی `run_id=2` و dry-run بدون تغییر `run_id=3` پس از ثبت checkpointها.
- افزودن mapping دائمی دسته‌بندی و override تک‌محصولی، flock جلوگیری از هم‌زمانی و ثبت تاریخچه اجرای sync در SQLite.
- تأیید تصویری چهار محصول در پنل Mixin همراه تصویر، قیمت، موجودی و دسته‌بندی صحیح.
- تکمیل پایلوت انتقال ۴ کالای واقعی به محصولات Mixin `223–226` و تصاویر اصلی `248–251`.
- حذف فاصله‌های اضافی داخل پرانتز نام کالا پیش از ایجاد در Mixin.
- تأیید تصویری قیمت و موجودی سه کالای باقی‌مانده و استفاده از نام محصول برای alt تصویر به‌جای عنوان قدیمی Picture.
- تأیید انتقال تصویر واقعی پد الکلی به Mixin image شماره `248` و عملکرد duplicate/permission guards.
- افزودن انتقال محدود تصویر محصول از URL محک به Base64 میکسین با preview، جلوگیری از duplicate و rollback مالکیت‌سنجی‌شده.
- افزودن فرمان امن `mahak:images:diagnose` برای بررسی ارتباط Picture/PhotoGallery بدون چاپ محتوای کامل تصویر.
- افزودن نرمال‌سازی حروف فارسی و فاصله‌های نام محصول و فرمان خلاصه `mahak:products:candidates`.
- تأیید انتقال اولین کالای واقعی محک (`11629315`) به محصول Mixin شماره `223` و کنترل تصویری قیمت، موجودی و وضعیت فعال.
- تأیید ثبت mapping برای update و جلوگیری guard از rollback در حالت مجوز بسته.
- افزودن preview/apply/rollback محدود به یک ProductDetail بدون خاموش‌کردن dry-run عمومی.
- تأیید تصویری قیمت `۳۶۰۰۰` ریال و موجودی `۵۲۰۰` برای پد الکلی در محک و بازارا.
- تأیید موفق چرخه ایجاد، خواندن و حذف محافظت‌شده محصول تست Mixin شماره `222` در دسته‌بندی موجود `6`.
- ثبت الزام واقعی دسته‌بندی هنگام ایجاد محصول، با وجود optional بودن آن در OpenAPI.
- نمایش بدنه خطاهای HTTP در CLI برای تشخیص validation API با حذف خودکار credentialها.
- افزودن بازرسی فقط‌خواندنی دسته‌بندی‌های Mixin و الزام دسته‌بندی موجود برای محصول تست.
- پنل مشتری، صف کارها، retry/backoff، circuit breaker و قفل worker در roadmap هستند.
- snapshot کامل OpenAPI محک و Mixin به منابع پروژه اضافه شد.
- وضعیت استقرار، تصمیم‌های قطعی و چک‌لیست ادامه پروژه مستند شد.
- ماتریس انتقال داده و smoke test کنترل‌شده محصول Mixin اضافه شد.

## 2026-09-19

- تأیید خواندن واقعی ۴ Product، ۲ ProductCategory، ۴ ProductDetail و ۴ VisitorProduct از محک.
- تأیید اتصال Mixin API v4 و پاسخ health نسخه `4.0.0`.
- رفع خطای type ناشی از تبدیل شناسه عددی ProductDetail به integer در کلیدهای آرایه PHP.
- اضافه‌شدن fallback قیمت از `VisitorProduct.Price` صفر به `ProductDetail.Price1`.
- trim شدن فاصله ابتدا و انتهای نام محصول.
- اضافه‌شدن فرمان امن `mahak:products:diagnose`.
- افزایش تست‌ها به ۶ مورد؛ صفر failure و صفر skipped.
- اجرای موفق dry-run با ۴ create، صفر update و صفر skip؛ بدون نوشتن production.
- ثبت نیاز به تأیید دستی واحد قیمت، `Count1` در برابر `Count2` و اولین اجرای تک‌محصولی.

## 2026-09-17

- تغییر همه endpointهای Mahak به نسخه‌های بدون V2 مطابق اعلام پشتیبانی.
- اضافه‌شدن فرمان امن `mahak:products:inspect`.
- ثبت فرایند ارسال اولیه کالا از بازارا.
- تکمیل راهنمای استقرار روی سرور لینوکسی و عیب‌یابی مسیر پروژه.
- مستندسازی تفاوت `MIXIN_BASE_URL` با مسیر `/api/v4`.

## 2026-09-15

- ایجاد ساختار اولیه Bridge با PHP 8.2+.
- اضافه‌شدن کلاینت‌های Mahak و Mixin.
- همگام‌سازی افزایشی محصول با checkpoint، mapping و snapshot در SQLite.
- اضافه‌شدن dry-run، HTTP health endpoint، Docker، CI و تست‌های mapper.
